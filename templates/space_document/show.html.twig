{% extends 'base.html.twig' %}

{% block title %}SpaceDocument{% endblock %}

{% block body %}
    <h1>SpaceDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ space_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ space_document.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_space_document_index') }}">back to list</a>

    <a href="{{ path('app_space_document_edit', {'id': space_document.id}) }}">edit</a>

    {{ include('space_document/_delete_form.html.twig') }}
{% endblock %}
